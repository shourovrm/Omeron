# CCE (code-context-engine)
# CCE local cache (per-machine, not for version control)
.cce/
# .mcp.json contains absolute paths regenerated by `cce init`
.mcp.json

# Local tooling / assistant config + build notes — not part of the published source
.claude/
CLAUDE.md
BUILD_PROMPT.md
DEVLOG.md

# Reference repos (not part of Omeron source)
stealth/
Infinity-For-Reddit/

# Android / Gradle
*.iml
.gradle/
**/build/
local.properties
.idea/
.DS_Store
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex

# Signing — never commit keystore or creds
*.jks
*.keystore
keystore.properties
signing.properties
